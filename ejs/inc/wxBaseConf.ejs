<script>
	var WXCONFIG = {
        appId: '${appId!}', // 必填，公众号的唯一标识
        timestamp: '${timestamp!}', // 必填，生成签名的时间戳
        nonceStr: '${nonceStr!}', // 必填，生成签名的随机串
        signature: '${signature!}'// 必填，签名，见附录1
	};
    var SHARE_CONFIG = {
        title: '<#if title?? && title !=''>${title?replace("\'", "\\'")!}</#if>', // 分享标题
        desc: '<#if desc?? && desc != ''>${desc?replace("<", "&lt;")?replace(">", "&gt;")?replace('\n','')?replace("\'", "\\'")!}</#if>', // 分享描述
        link: '${link!}', // 分享链接
        imgUrl: '${imgUrl!}' // 分享图标
    };
    var STAT_CONFIG = {
        common: {},
        custom: {
            module: '${module!}',
            type: '${statType!}'
        }
    };
    var appConfigId = '${appConfigId!}';
	var DEBUG = false;
	var staticurl = '${staticurl}';
</script>